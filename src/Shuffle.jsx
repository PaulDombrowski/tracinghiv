import React from 'react';

export default function Shuffle() {
  return (
    <section>
      <p>
        Discover at random. Later: a button to fetch a random item from the database and show a preview.
      </p>
      {/* TODO: Random button + preview */}
    </section>
  );
}